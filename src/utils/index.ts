// src/utils/index.ts
// Export all utility functions

export {
  formatCurrency,
  formatCurrencyWithCents,
  formatDuration,
  formatTimestamp,
  formatDate,
  formatDatetime,
  formatRelativeTime,
  formatNumber,
  formatPercentage,
  formatPhoneNumber,
  truncateText,
} from './format';
