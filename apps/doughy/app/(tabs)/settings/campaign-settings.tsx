// app/(tabs)/settings/campaign-settings.tsx
// Campaign settings route (PostGrid, Meta OAuth)
import { CampaignSettingsScreen } from '@/features/campaigns/screens/CampaignSettingsScreen';

export default CampaignSettingsScreen;
