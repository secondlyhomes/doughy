{
  "_comment": "Add these configurations to your app.json file for web/PWA support",
  "expo": {
    "web": {
      "bundler": "metro",
      "output": "static",
      "favicon": "./assets/favicon.png",
      "serviceWorker": {
        "enabled": false,
        "_comment": "Enable in production only. Service workers can complicate debugging in development."
      },
      "config": {
        "firebase": {
          "_comment": "Optional: Add Firebase config for web analytics, auth, etc."
        }
      }
    },
    "plugins": [
      [
        "expo-router",
        {
          "web": true,
          "_comment": "Enable web support for Expo Router"
        }
      ]
    ]
  }
}
