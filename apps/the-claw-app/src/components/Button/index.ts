/**
 * Button Module
 *
 * Accessible button component with variants (primary, secondary, text)
 * and sizes (sm, md, lg).
 */

export { Button } from './Button';
export type { ButtonProps, ButtonVariant, ButtonSize } from './types';
