#!/usr/bin/env sh

echo "Running pre-commit checks..."

# 1. Secret scanning
echo "Scanning for secrets..."
node scripts/check-secrets.js || exit 1

# 2. TypeScript check (warning only — CI enforces strictly)
echo "Type checking..."
npx tsc --noEmit 2>&1 || echo "Warning: Type errors found. CI will enforce type-checking."

echo "Pre-commit checks passed!"
