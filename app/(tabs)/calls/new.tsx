// app/(tabs)/calls/new.tsx
// New Call — select contact and generate pre-call briefing
// For now, redirect to pre-call with a placeholder
import { PreCallScreen } from '@/features/callpilot';
export default PreCallScreen;
